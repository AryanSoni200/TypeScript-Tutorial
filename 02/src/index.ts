class User {
    name: string
    email: string
    constructor(name: string, email: string) {
        this.name = name,
        this.email = email
    }
}

const user = new User("Aryan", "aryan@gmail.com")

